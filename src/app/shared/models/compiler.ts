import { Question } from './question';

export class Compiler {
    language: string;
    question= {
        id: Number,
        template:null
    }
    user = {
        "userId":null
    }
    input: string;
}
